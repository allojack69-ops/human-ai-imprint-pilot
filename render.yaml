services:
  - type: web
    name: human-ai-imprint-pilot-v1-2
    runtime: docker
    plan: free
    envVars:
      - key: ADMIN_KEY
        generateValue: true
      - key: DATABASE_URL
        sync: false
    healthCheckPath: /health
